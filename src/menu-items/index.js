/* eslint-disable */
import userDashboard from "./userDashboard";

// ==============================|| MENU ITEMS ||============================== //

export const dashboard = userDashboard;
export { userDashboard };

const menuItems = {
  items: [userDashboard]
};

export default menuItems;
